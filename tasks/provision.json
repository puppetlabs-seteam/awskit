{
  "puppet_task_version": 1,
  "supports_noop": true,
  "description": "Provisions demo Puppet infrastructure",
  "parameters": {
    "type": {
      "description": "Instance type to create",
      "type": "Enum[master, linux_node, windows_node, discovery, windc, wsus, cd4pe]"
    },
    "name": {
      "description": "Instance name, auto-assigned if left blank",
      "type": "Optional[String[6]]"
    },
    "region": {
      "description": "AWS region",
      "type": "Enum[us-west-2, eu-west-2]"
    },
    "count": {
      "description": "Number of instances to create, defaults to 1",
      "type": "Optional[Integer]"
    }
  }
}
