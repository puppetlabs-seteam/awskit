---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "User-level data"
    path: "%{::user}.yaml"

  - name: "AWS region-level data"
    path: "%{::aws_region}.yaml"

  - name: "Common data"
    path: "common.yaml"
